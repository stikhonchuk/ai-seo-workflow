# Content Audit Utility v2.0

Python утилита для SEO-анализа контента [YOUR-DOMAIN]: аудит страниц, анализ keyword gaps, оптимизация CTR, обнаружение каннибализации.

## Возможности

### Базовый аудит
- ✅ Парсинг sitemap.xml (фильтрация блога и категорий)
- ✅ Сбор данных со страниц (title, H1, meta description, количество слов)
- ✅ Извлечение топ-10 ключевых слов (без русских стоп-слов)
- ✅ Обогащение данных из Yandex Webmaster и Google Search Console
- ✅ Генерация отчётов: CSV, JSON, Markdown
- ✅ Кэширование для инкрементального обновления
- ✅ Логирование процесса

### SEO Gap Analysis (v2.0)
- 🔍 **Keyword Gap Analysis** — запросы с показами, но без контента на сайте
- 📈 **CTR Optimization** — страницы с высокими показами, но низким CTR
- ⚠️ **Cannibalization Detection** — страницы, конкурирующие за одни ключевые слова

## Установка

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r scripts/content_audit/requirements.txt
```

## Использование

### Полный аудит (все этапы)

```bash
venv/bin/python scripts/content_audit/main.py --full
```

Выполняет:
1. Парсинг sitemap.xml
2. Скрейпинг ~585 страниц (блог + категории)
3. Извлечение ключевых слов
4. Обогащение данными GSC
5. Генерация отчётов

⏱️ Время выполнения: ~10 минут

### Только sitemap

```bash
venv/bin/python scripts/content_audit/main.py --sitemap-only
```

Быстрый просмотр структуры сайта без скрейпинга.

### Обновить только GSC данные

```bash
venv/bin/python scripts/content_audit/main.py --update-webmaster
```

Использует кэш страниц, обновляет только метрики из webmaster отчётов.

### Опции

- `--output csv` — только CSV отчёт
- `--output json` — только JSON отчёт
- `--output both` — оба формата (по умолчанию)
- `--force-refresh` — игнорировать кэш, обновить все страницы

## Выходные файлы

```
research/content-audit/
├── site-content-audit-2026-01-31.csv   # Основной отчёт (Excel/Sheets)
├── site-content-audit-2026-01-31.json  # Полные данные + статистика
├── content-gaps-2026-01-31.md          # Анализ гэпов в контенте
├── site-content-audit-latest.csv → site-content-audit-2026-01-31.csv  # Symlink к последнему
├── site-content-audit-latest.json → site-content-audit-2026-01-31.json
├── content-gaps-latest.md → content-gaps-2026-01-31.md
├── audit-log.txt                       # Лог выполнения
└── .cache.json                         # Кэш (для инкрементального обновления)
```

**Примечание:** Файлы сохраняются с датой (`YYYY-MM-DD`), что позволяет накапливать историю аудитов. Symlinks `*-latest.*` всегда указывают на самый свежий отчёт для удобного доступа.

## CSV формат

| Колонка | Описание |
|---------|----------|
| url | Адрес страницы |
| lastmod | Дата последнего изменения |
| content_type | blog / collection |
| title | Заголовок страницы |
| h1 | Первый H1 |
| meta_description | Мета-описание |
| word_count | Количество слов |
| top_keywords | Топ-10 ключевых слов (через запятую) |
| yandex_clicks | Клики (Yandex Webmaster) |
| yandex_impressions | Показы (Yandex Webmaster) |
| yandex_ctr | CTR% (Yandex Webmaster) |
| yandex_position | Средняя позиция (Yandex Webmaster) |
| gsc_clicks | Клики (GSC) |
| gsc_impressions | Показы (GSC) |
| gsc_ctr | CTR% (GSC) |
| gsc_position | Средняя позиция (GSC) |
| total_clicks | Всего кликов (Yandex + GSC) |
| total_impressions | Всего показов (Yandex + GSC) |
| status | ok / error / no_content |
| error | Сообщение об ошибке (если есть) |

## Архитектура

```
scripts/content_audit/
├── main.py                 # CLI точка входа
├── sitemap_parser.py       # Парсинг sitemap.xml
├── page_scraper.py         # Скрейпинг страниц
├── keyword_extractor.py    # Извлечение ключей
├── webmaster_data.py       # Парсинг Yandex/GSC
├── report_generator.py     # Генерация отчётов
└── requirements.txt        # Зависимости
```

## Примечания

- **Scope:** Анализируются только /blogs/ и /collection/ (~585 страниц)
- **Delay:** 0.5 сек между запросами (вежливость к серверу)
- **Keywords:** Простая частотность (без TF-IDF)
- **Cache:** Использует lastmod для определения изменённых страниц
- **GSC:** Автоматически ищет последний отчёт в research/webmasters/

## Интеграция в рабочий процесс

### Рекомендуемая частота запуска

**Перед ежемесячным отчётом** (последний день месяца):
```bash
# Запустить полный аудит для получения актуальных данных
venv/bin/python scripts/content_audit/main.py --full
```

Это позволяет:
1. **Обновить метрики** для monthly report (текущие позиции, клики, показы)
2. **Планировать контент** на следующий месяц без дублирования
3. **Отслеживать прогресс** - какие статьи набирают трафик
4. **Находить gaps** - страницы, требующие доработки

### Workflow интеграция

1. **Последний день месяца:**
   - Запустить `--full` аудит (~10 минут)
   - Получить свежие данные для отчёта

2. **Создание monthly report:**
   - Использовать `site-content-audit.csv` для метрик по страницам
   - Использовать `content-gaps.md` для анализа слабых мест

3. **Планирование следующего месяца:**
   - Открыть `site-content-audit.csv`
   - Проверить `top_keywords` существующих статей
   - Избежать дублирования тем
   - Выявить недостающие темы

4. **В течение месяца (опционально):**
   - Запустить `--update-webmaster` для обновления только метрик GSC
   - Быстрее чем `--full` (использует кэш страниц)

## Использование в Claude Code

После генерации отчётов, CSV файл можно открыть и использовать для:

1. **Проверка дублирования** перед созданием нового контента
2. **Анализ content gaps** - страницы с низким количеством слов
3. **Топовые ключевые слова** существующего контента
4. **Эффективность страниц** по метрикам GSC
5. **Планирование календаря** на следующий месяц

Пример:
```bash
# Посмотреть топ страницы по кликам
cat research/content-audit/content-gaps.md

# Найти статьи про конкретную тему
grep -i "лоферы" research/content-audit/site-content-audit.csv
```

---

## Дополнительная документация

Подробный workflow для интеграции в ежемесячный процесс: [`.claude/workflows/monthly-content-audit.md`](../../.claude/workflows/monthly-content-audit.md)
